import React from 'react';
import { observer } from 'mobx-react';

interface Props {}

const anchor = 'TemplateNameListItem';

const _TemplateNameListItem = ({}: Props) => <></>;

export const TemplateNameListItem = observer(_TemplateNameListItem);
